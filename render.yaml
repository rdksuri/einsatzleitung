services:
  - type: web
    name: einsatzleitung
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: EINSATZ_PIN
        sync: false
      - key: JWT_SECRET
        generateValue: true
